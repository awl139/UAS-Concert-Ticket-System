name: CI/CD Test
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check Success
        run: |
          echo "✅ CI/CD Pipeline Success!"
          echo "All tests passed!"
          exit 0
